# Radxa 极限精简模式：协议优先 + 预合成音频，LLM 回退到 null
# 仅覆盖与 base.yaml 不同的字段

llm:
  backend: null
  ctx: 768
  threads: 2
  temperature: 0.2

speech:
  tts:
    threads: 1
    max_chars: 48
    cache_size: 40

rag:
  max_distance: 0.38

hardware:
  enable_precomputed_audio: true
  perf_warning_mb: 700
