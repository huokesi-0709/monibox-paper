# 纯文本最小验证模式（无语音链路）
# 仅覆盖与 base.yaml 不同的字段

app:
  mode: text

speech:
  tts:
    backend: sherpa             # 文本模式下不启用，保留默认值即可
    cache_size: 20

hardware:
  enable_led: false
  enable_screen: false
  enable_precomputed_audio: false
