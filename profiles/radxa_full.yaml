# Radxa 完整模式：ASR + protocol + RAG + local rewrite + TTS
# 仅覆盖与 base.yaml 不同的字段

llm:
  ctx: 1536
  threads: 4
  temperature: 0.25

speech:
  tts:
    threads: 2
    max_chars: 76
    cache_size: 80

hardware:
  enable_precomputed_audio: true
  perf_warning_mb: 900
